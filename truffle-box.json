{
  "ignore": [
    "README.md"
  ],
  "commands": {
    "Compile contracts": "yarn compile",
    "Migrate contracts": "yarn migrate",
    "Test contracts": "yarn test"
  },
  "hooks": {
    "post-unpack": "yarn install && yarn create-new-sale"
  }
}
